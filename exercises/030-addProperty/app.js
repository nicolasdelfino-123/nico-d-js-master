function addProperty(obj, key) {
  // your code here
  obj[key] = true;
  return obj;
}
/* Escribe una función llamada addProperty. Dado un objeto y una key, addProperty agrega la key al objeto con el valor true.
Luego, la función tiene que devolver el objeto. */
