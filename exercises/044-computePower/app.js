function computePower(num, exponent) {
  // your code here
  return num ** exponent;
}

let output = computePower(2, 3);
console.log(output); // --> 8

/* Escribe una función llamada computePower. Dado un número y un exponente, computePower retorna el número, elevado al exponente. */
