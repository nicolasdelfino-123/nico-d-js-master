// Write your function here Escribe una función llamada computeAreaOfATriangle. Dada la base y altura de un triangulo, computeAreaOfATriangle retorna el área de un triángulo.

function computeAreaOfATriangle(b, a) {
  return (b * a) / 2;
}
