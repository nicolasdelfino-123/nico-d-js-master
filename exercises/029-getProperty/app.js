function getProperty(obj, key) {
  // your code here
  return obj[key];
}

/* Escribe una función llamada getProperty. Dado un objeto y una key, getProperty retorna el valor de la propiedad con esa key. */
