function isPersonOldEnoughToVote(person) {
  // Add your code after this line
  if (person.age >= 18) return true;
  return false;
}

/* Escribe una función llamada isPersonOldEnoughToVote. Dado un objeto person, que contiene una propiedad age, isPersonOldEnoughToVote devuelve true si la persona dada tiene la edad suficiente para votar. De lo contrario retorna false. */
