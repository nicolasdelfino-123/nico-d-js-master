function addFullNameProperty(obj) {
  // Add your code after this line
  obj.fullName = obj.firstName + " " + obj.lastName;
  return obj;
}

/* Escribe una función llamada addFullNameProperty. Dado un objeto que tiene una propiedad firstName y una propiedad lastName, addFullNameProperty agrega la propiedad fullName al objeto, su valor es una string con el nombre y el apellido separados por un espacio. */
