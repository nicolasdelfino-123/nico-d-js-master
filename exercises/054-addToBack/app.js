function addToBack(arr, element) {
  // your code here
  arr.push(element);
  return arr;
}

let output = addToBack([1, 2], 3);
console.log(output); // -> [1, 2, 3]

/* Escribe una función llamada addToBack. Dado un array y un elemento, addToBack retorna el array con el elemento añadido al final. */
