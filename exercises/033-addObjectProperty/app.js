function addObjectProperty(obj1, key, obj2) {
  // Add your code after this line
  obj1[key] = obj2;
  return obj1;
}

/* Escribe una función llamada addObjectProperty. Esta función toma dos objetos y una clave (key) como argumentos.

La función addObjectProperty debe establecer una nueva propiedad en el primer objeto utilizando la clave (key) proporcionada. 

El valor de esta nueva propiedad debe ser el segundo objeto.

La función addObjectProperty debe devolver el primer objeto modificado con la nueva propiedad. */
