function getNthElement(array, n) {
  // Add your code after this line
  return array[n];
}
/*Escribe una función llamada getNthElement. Esta función recibe dos argumentos:

Un array.
Un número entero n.
La función debe devolver el elemento del array que se encuentra en la posición n. Recuerda que las posiciones en los arrays comienzan desde 0 (cero), por lo que el primer elemento está en la posición 0, el segundo en la posición 1, y así sucesivamente.

📎 Ejemplo:

javascript
Copiar
Editar
let output = getNthElement([1, 3, 5], 1);
console.log(output); // --> 3 (porque el elemento en la posición 1 es 3) */
