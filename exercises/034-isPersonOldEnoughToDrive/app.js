function isPersonOldEnoughToDrive(person) {
  // Add your code after this line
  if (person.age >= 16) {
    return true;
  }
  return false;
}

/* Escribe una función llamada isPersonOldEnoughToDrive.
 Dado un objeto person, que contiene una propiedad age, isPersonOldEnoughToDrive devuelve true la persona dada tiene la edad suficiente para conducir. De lo contrario retorna false. */
