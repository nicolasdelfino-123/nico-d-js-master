function addArrayProperty(obj, key, arr) {
  // Add your code after this line
  obj[key] = arr;
  return obj;
}

/* Escribe una función llamada addArrayProperty.

addArrayProperty recibe un objeto, un string que representa la key y un array.

addArrayProperty establece una nueva propiedad en el objeto con la key dada,

cuyo valor debe ser el array dado. */
