// Write your function here
/* Escribe una función llamada isEvenLength. Dada una palabra, isEvenLength retorna true si la cantidad de letras que tiene la palabra es par, de lo contrario retorna false. */

function isEvenLength(w1) {
  return w1.length % 2 === 0;
}
