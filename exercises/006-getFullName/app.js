/* Escribe una función llamada getFullName. Dado un nombre y un apellido, getFullName retorna un string con el nombre y apellido separados por un espacio. */

function getFullName(firstName, lastName) {
  // your code here
  return `${firstName}  ${lastName}`;
}
